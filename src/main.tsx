import React from "react";
import ReactDOM from "react-dom/client";
import PlanPage from "../Frontend/PlanPage";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PlanPage />
  </React.StrictMode>
);
